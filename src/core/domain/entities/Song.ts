export interface Song {
    id: string;
    title: string;
    author: string; // El artista o compositor de la canción
    lyrics: string;
    baseChords: string; // Los acordes originales subidos por el líder
}